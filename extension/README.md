# Legacy prototype

This folder is the **old** autofill prototype (no popup, no learning, no state machine).

The production Apply Agent lives in:

`../application-auto-agent/`

See that README for install + test instructions.
